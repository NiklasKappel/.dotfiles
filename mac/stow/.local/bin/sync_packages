#!/bin/bash
set -euo pipefail

brewfile_path="${HOME}/.dotfiles/mac/backups/Brewfile"

echo "Updating package list..."
brew update

echo "Syncing packages..."
brew bundle --file="${brewfile_path}" --cleanup

echo "Done."
