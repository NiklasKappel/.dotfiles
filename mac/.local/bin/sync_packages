#!/bin/bash
set -euo pipefail

brewfile_path="${HOME}/.local/share/dotfiles/Brewfile"

echo "Updating package list..."
brew update

echo "Syncing packages..."
brew bundle --file="${brewfile_path}" --force

echo "Done."
