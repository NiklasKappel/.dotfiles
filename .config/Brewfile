# See: https://matthiasportzel.com/brewfile/
# Sync using: `brew update && brew bundle install --cleanup --file=~/.config/Brewfile && brew upgrade`

brew "fish"
brew "neovim"
brew "ripgrep"
brew "stow"
brew "tree"

cask "hyper"
cask "linearmouse"
cask "middleclick"

# The packages below are not installed.
# cask "adguard"  # Requires sudo. Use the adguard Safari extension instead.
# cask "hiddenbar"  # Not verified by Apple, purely cosmetic.
